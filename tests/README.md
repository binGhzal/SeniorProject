# Tests

This folder contains the project test suite.

- `test_runtime.py`: core unit and integration-style tests for runtime logic.

Run from repository root:

```bash
python -m unittest tests/test_runtime.py -v
```
