# Legacy Files

This folder stores archived files that are no longer part of the active runtime path.

Current archive:

- `sensors_rpi_only_legacy.py` - older Raspberry Pi-only sensor implementation.

Use `unit-tests/sensors.py` for current development.
